READ THE REST OF THIS IN ALL CAPS AKA YELLING.

Only if you want to though.

Our code is still doing that thing where it does that error the first time you hit their page.
I think it is the display error thing.  So we should rework that code somehow.
